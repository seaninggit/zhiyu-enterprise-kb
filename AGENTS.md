# 知域企业知识库工程章程

本文件适用于整个仓库。任何需求分析、设计、编码、测试、迁移、部署、运维和迭代工作都必须遵守。代码与当前真实运行证据高于历史说明；若 README、交接文字、测试断言和运行状态冲突，先核验并修正文档或实现，不得选择性引用。

## 1. 产品目标与责任边界

- 将知域建设为可落地的企业知识生产、审批发布、权限检索、AI问答和持续治理系统，不按演示项目标准开发。
- 核心闭环：上传资料 → 解析与人工确认 → 提交审批 → 独立审核通过或驳回 → 当前负责人修订 → 再次提交 → 审批发布 → 权限检索与引用 → 反馈治理 → 修订发布后闭环。
- 人员停用、离职或调岗前必须明确继任人，批量转交其负责资料和未完成治理任务，同时保留历史上传人、审批人、版本和审计。
- 管理员负责审批、治理和异常路由，不承担日常代编辑。
- 当前阶段以“在现有D1架构内把普通RAG做实，并交付受控自主规划 Agent V1”为目标；Agent 首个场景仅限制度到期治理，产品事实以 `PRODUCT.md` 为准。

## 2. 本期明确不做

- 专业向量库迁移。
- OA、HR、财务或外部审批系统连接。
- Agent 自动执行发布、审批、作废、删除、责任转交或权限变更。
- 多模型管理页面、完整模型路由治理和完整 PromptOps 生命周期前端。

已有可复用后端基础可以保留，但前端入口必须隐藏、只读或明确标为非正式能力。除非用户重新明确授权，不得扩展上述范围。

### 2.1 本期 Agent V1 强制边界

- 定时器和业务事件只能提交目标、范围与上下文，不得在入口中实现制度到期治理步骤。
- 自主规划必须产生持久化运行、步骤、证据与工具调用；仅有模型问答或固定 `if` 流程不得标记为 Agent 完成。
- 工具按只读、低风险自动执行和高风险待确认分级。低风险仅限可撤销的治理任务、站内通知和审计；高风险动作必须暂停。
- Agent 不得使用固定邮箱、人员、部门、文档标题、测试账号或场景答案。接收人和作用域必须来自授权上下文与持久化配置。
- 运行必须支持幂等、预算、超时、失败、重试、人工拒绝、恢复与复核闭环。

## 3. 开始工作前

1. 运行并阅读 `git status --short`、`git diff --stat` 和相关 `git diff`。
2. 确认分支、HEAD、远程状态、相关代码、数据库与运行状态。对易变化事实重新核验。
3. 工作区可能很脏。所有无关修改都属于用户，不得覆盖、回滚、整理或顺手提交。
4. 不得输出 `.dev.vars`、令牌、密钥、个人信息或敏感业务正文。
5. 不得提交 `.local-backups/`、`demo-files/`、隔离验收文件、临时探针、构建诊断或秘密配置。
6. 禁止 `git reset --hard`、`git checkout --`、批量清理和不明确目标的破坏性命令。
7. 先界定本次是分析、实现、验收、部署还是运营任务；只要求分析时不得顺带改生产或外部状态。

## 4. 通用工程原则

- 禁止业务答案写死：不得根据“北京”“材料”“报销”、具体标题、人员、部门、测试账号或验收问题进入专用检索分支、降低阈值或返回固定答案。
- 安全、协议、输入校验和通用会话边界可以使用少量确定性规则，但必须与具体业务无关、可测试，并尽量可配置。
- 所有效果必须来自可迁移的技术机制：数据结构、Metadata、权限、结构化解析、哈希、版本化索引、混合召回、重排、证据校验、任务状态、配置或模型。
- 换一套企业资料、删除演示数据后，功能仍应成立。
- 前端显示必须对应真实持久化状态；不得把保存配置当作任务执行，把关键词可用当作向量完成，把排队当作已发送，把草稿当作已发布。
- 失败要可观察、可解释、可恢复。不得吞掉影响业务真实性的错误。
- 设计必须覆盖正常、空状态、加载、失败、降级、无权限、重试、重复提交、并发和恢复路径。

## 5. 数据、清洗、版本与文件

- 原文件、解析正文、清洗结果、发布快照、切片和向量是不同层次，必须可追溯并可重建。
- 使用文件哈希、规范化文本哈希和近重复候选识别重复；最终合并、替代或作废由负责人确认。
- 清洗保留标题、章节、条款、列表、表格、脚注、附件关系和原文定位，不得用简单正则破坏制度结构。
- 图片和扫描PDF首次解析必须人工确认；关键名称、金额、日期、编号和表格需重点复核。
- 不支持格式可以保留原件，但必须补充可检索正文后才能进入知识流程；空文件必须拒绝。
- 草稿修改生成新版本，未审批版本不得污染已发布快照或RAG索引。
- 建立生效、失效、替代、适用部门、地区、岗位、人群、优先级和例外等Metadata时，使用数据字段，不使用问题关键词推断固定结论。

## 6. RAG与AI质量

- 统一生产 embedding 模型、维度、模型标识和索引版本。记录内容哈希、文档版本、状态、错误和时间。
- 只有当前发布版本的全部有效切片都有维度、模型、哈希一致的向量，才能标记为向量索引完成；否则真实标记为等待、部分、失败、过期或关键词就绪。
- 切分采用章节 → 条款 → 段落 → 句子 → 长度兜底的递归结构，支持父子片段、表头继承、相邻上下文和去重；固定字符切分只能作为兜底。
- 检索顺序应包括权限和有效版本过滤、查询理解、企业术语扩展、Metadata过滤、关键词与向量召回、候选合并、去重、Reranker、多样性控制、父级扩展和证据充分性判断。
- 多轮追问必须在完整授权范围重新检索；上一轮实体和引用只作理解与排序信号，不得成为硬过滤条件。
- 同音或错字候选用于扩展检索，不得自动覆盖用户明确意图；用户纠正优先。
- 企业事实、通用建议和待确认事项必须分开。没有依据、证据冲突、范围不明、模型失败或索引降级时必须真实说明，不得补写公司规则。
- 引用必须落到用户有权访问的有效文档版本和具体片段；校验数字、日期、否定、义务强度、地区、部门、岗位、人群、范围和例外。
- 权限在检索前执行，来源打开、下载和导出时再次校验。不得依赖生成后的过滤防泄露。

## 7. 前端与企业体验

- 页面按用户任务和职责归属，不按后端表结构堆叠。知识服务、审批、治理、组织、系统运行和安全模块边界清晰。
- 用户始终能理解当前状态、负责人、原因、下一步和操作后果。
- 普通员工看到业务语言；管理员在运行与质量页面查看技术细节。
- AI回答展示依据、版本、章节/条款、适用范围、查看原文和反馈入口；不得展示内部思维链。
- 上传与详情展示原件、解析、OCR、敏感检查、重复/冲突、Metadata、切片和索引等真实质量状态，并说明是否阻止提交或发布。
- 管理端应提供解析/OCR、重复/冲突、Metadata缺失、索引异常等可执行队列，而非只有统计数字。
- 所有交互必须有键盘焦点、可访问名称、禁用/加载反馈、合理字号、响应式布局，并真实检查乱码、重叠和窄屏。

### 7.1 前端设计与体验合同

- `harness/frontend-quality-contract.json` 是前端最低质量合同。新增样式优先使用语义化 Design Tokens，不得继续扩大小字号、硬编码颜色、任意层级和 `!important` 等历史债务。
- 正文和控件默认不得小于 12px，交互目标不得小于 32px；现有更小字号属于已登记债务，只允许减少，不得把债务上限调高规避治理。
- 颜色不单独传达状态；正文对比度目标至少 4.5:1，大字号至少 3:1。错误、成功、警告、禁用、选中和焦点需要视觉与文本/图标双重表达。
- 表单、按钮、表格、标签、弹窗、抽屉、分页、提示和状态卡遵循统一层级、间距、圆角、文案及交互状态；同一动作不得在不同模块使用冲突名称。
- 所有业务页面覆盖默认、悬停、焦点、禁用、加载、空、错误、无权限和降级状态；危险操作说明对象、范围、后果和恢复方式。
- 在 1440×900、1280×800、768×1024、390×844 视口以及 200% 缩放下真实检查；覆盖长中文、英文、数字、无空格字符串、空值和大数据量。
- 视觉验收必须检查重叠、截断、横向滚动、遮挡、焦点顺序、键盘闭环、可访问名称、对比度、减少动效和弹窗焦点管理。静态 CSS 扫描不能替代浏览器证据。
- 页面性能关注首屏、交互延迟、布局偏移、主包体积、图片/字体和长列表；达到阈值前先测量再优化，不以主观“感觉流畅”验收。

## 8. 权限、安全与审计

- 每个API读写操作都在服务端鉴权，区分 VIEW、EDIT、APPROVE、GOVERN和SYSTEM责任。
- 审核权限不等于编辑权限，申请人、修改人、负责人和审批人不得混淆。
- 文档、空间、部门、用户和用户组权限使用统一行级策略，并处理ACL有效期。
- 敏感策略分别覆盖存储、索引、第三方embedding、模型上下文、日志、分析和引用展示。
- 提示词注入、跨部门访问、停用账号、越权转交、越权授权、删除和过期ACL必须做负向测试并核对数据未被改变。
- 高风险发布、审批、权限、转交和删除必须由正式工作流和人工确认完成，AI只能建议。
- 关键动作写入审计，通知保留站内记录、邮件投递状态、失败原因、重试载荷和请求ID。

## 9. 数据库、任务与Cloudflare

- 所有持久化变更使用有序、可审计迁移。执行前分别检查本地与远程迁移记录和真实schema。
- 不得在已有库迁移记录缺失时盲目重放全部迁移；先制定目标迁移、备份、兼容和恢复方案。
- 后台任务必须幂等、可重试、分批、可续跑，并持久化运行状态、开始/结束时间、详情、错误和请求ID。
- 定时任务配置不等于真实运行；必须通过真实Scheduled入口、运行记录和业务/审计结果验收。
- Workers中不得保留跨请求可变状态，不得遗留浮空Promise；外部提供商调用要有超时、降级、错误和成本记录。
- 恢复验收必须检查D1、原始对象、发布快照和派生索引的一致性。

## 10. 测试与真实运行证据

- 每项重要结论必须说明证据类型：代码、自动化测试、本地运行、真实前端、本地D1、生产HTTP/UI、远程D1或部署记录。
- 单元测试或代码存在不能代替真实前端和运行时验收。
- 验收账号、角色、资料和业务配置从前端创建；不得直接向业务数据库写死演示成功数据。
- 使用真实多角色场景，覆盖主流程、驳回再提交、版本隔离、继任转交、反馈闭环、权限负向和通知。
- AI验收覆盖多轮、范围变化、错别字、简称、无依据、冲突、权限、OCR/表格、模型与embedding降级、引用和提示词注入。
- 负向测试前后对比版本、负责人、删除状态、权限和审计，证明未发生越权副作用。
- RAG变更维护版本化黄金评测集，至少统计Recall@K、排序质量、引用准确率、声明支持率、拒答准确率、权限泄露率、关键数字错误、P50/P95延迟、成本和向量覆盖率。
- 运行 `node tests/engineering-guardrails.test.mjs`、相关测试、构建、lint/type检查和 `git diff --check`；若项目已有历史lint债务，区分存量与新增，不得隐瞒。

## 11. 部署与 GitHub

- 生产部署和GitHub同步必须等本次要求的全部验收通过后进行；分析和局部开发不自动授权部署。
- 部署前核对diff、暂存范围、迁移、备份/恢复、构建产物、绑定、域名、Cron和密钥引用，dry-run检查不得夹带备份或演示文件。
- 顺序：验收 → 远程迁移核验/应用 → 构建与dry-run → 部署 → 线上冒烟与权限负向 → 监控 → 精确提交和推送 → 更新/合并PR（按授权）。
- 线上冒烟至少覆盖主域名、www域名、关键API、静态资源、权限拒绝、数据库结构和核心运行指标；不得为冒烟写生产业务数据，除非用户明确授权。
- 发布前明确回滚路径：上一Worker版本、迁移兼容性、写入冻结策略、对象与索引恢复。
- 提交只包含本次确认文件。不得 `git add -A` 处理脏工作区。
- 最终报告生产版本、迁移状态、冒烟证据、提交、分支、PR/main同步状态、未提交本地文件、残余风险和监控建议。

## 12. 0→1与1→N迭代

- 0→1先证明真实用户闭环、安全边界、可恢复性和可衡量价值，不以页面数量为完成标准。
- 1→N优先处理生产证据最强的问题：频率、影响、风险、成本、依赖和可逆性。
- 扩展到更多部门、企业或数据规模前，核验组织差异、角色配置、租户隔离、迁移自动化、并发、区域合规、成本预算、支持责任和可观测性。
- 不为技术时髦提前引入复杂基础设施；当质量、容量或运维数据达到明确阈值时再演进。
- 每轮迭代都要形成：问题证据 → 假设 → 方案 → 验收指标 → 灰度/发布 → 监控 → 复盘 → 下一轮。

## 13. 完成定义

只有同时满足以下条件才可称为完成：目标用户能走通真实流程；越权用户不能操作或推断；数据库、版本、责任人、审计和通知正确；失败和降级真实可恢复；自动化及运行时验收通过；迁移与回滚明确；部署后线上冒烟通过；GitHub与生产状态已说明；残余风险和监控项已交接。

## 14. Harness 自动化入口

- 日常快速检查运行 `npm run harness:quick`。
- PR/CI 完整检查运行 `npm run harness:ci`；GitHub 的 `Engineering Harness` 必须作为受保护分支必过检查配置，工作流文件存在不等于仓库保护规则已开启。
- 发布前运行 `npm run harness:release`，它只验证发布就绪并执行 Wrangler dry-run，不会部署。
- 真实验收从 `harness/acceptance-template.json` 复制到被忽略的 `harness/evidence/`，逐项附真实 UI、API、D1、Scheduled 或生产证据，再用 `npm run harness:acceptance -- <文件>` 校验；模板和代码存在不能自动把状态改成通过。
- RAG 变更必须满足 `harness/rag-evaluation-contract.json`；合同只定义类别、指标和门槛，企业问题、账号、资料和预期来源必须通过真实产品流程创建，禁止在仓库中编码预期业务答案。
- 每次 Harness 执行在被忽略的 `harness/reports/` 生成可机读报告。报告只证明其中实际执行的检查，不代表真实前端、Scheduled、生产部署或线上冒烟已经完成。
- `check:maintainability` 以当前历史大文件为显式基线，禁止继续无边界增长，并限制新增生产文件体积；基线是待偿还债务上限，不是推荐文件大小，不得随意调高来绕过拆分。
- 生产部署必须通过 `scripts/deploy.sh` 的显式确认，并以 `wrangler.production.jsonc` 为单一生产配置源；不得在脚本内临时重写域名、D1、Cron 或绑定。

## 15. 全域控制矩阵

- 每次声称“完整”“全部”或“可上线”前，必须检查 `harness/control-matrix.json`，不得只审计用户最近提到的领域。
- 控制矩阵覆盖产品范围、业务状态机、前端体验、API、数据生命周期、权限安全、隐私合规、AI/RAG、文件处理、后台任务、通知集成、性能容量、可靠性恢复、可观测性、供应链与秘密、成本配额、发布、可维护性及交接。
- 自动化、源码、运行时和生产证据不可相互替代。没有能力在当前环境验证的控制必须保持 pending 并说明责任人和验证方法，不得删除检查项或降级证据级别。
- 新增领域、外部依赖或重大风险时，先更新控制矩阵、验收清单和门禁，再实现功能；发现遗漏时修正体系，不只修复单一现象。
