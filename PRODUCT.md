# 知域企业知识库产品事实

## 当前产品目标

知域面向企业知识生产、审批发布、权限检索、AI 问答和持续治理。当前开发目标是在既有知识治理闭环上增加一个受控的自主规划 Agent V1，首个场景为“制度到期治理”。

## 本期已确认范围

- Agent 由定时信号或明确的业务事件唤醒，接收治理目标和配置，不接收写死的业务步骤。
- Agent 在授权部门与有效文档范围内自主选择只读工具和低风险工具，形成有证据的处理计划。
- Agent 可以自动创建可撤销的治理任务、站内通知和审计记录。
- Agent 运行、规划、工具调用、证据、错误、成本和状态必须持久化，可查看、可重试、可恢复。
- 发布、审批、作废、删除、责任转交和权限变更只能生成待确认动作；人工确认后仍由正式业务服务执行。
- 证据不足、权限不足、证据冲突、模型不可用或达到预算时停止，并给出明确原因。

## 本期不做

- 专业向量数据库迁移。
- OA、HR、财务或外部审批系统连接。
- Agent 自动执行发布、审批、作废、删除、责任转交或权限变更。
- 通用多智能体平台、任意代码执行或无边界外部工具调用。
- 完整 PromptOps 和多模型管理前端。

## 完成标准

只有以下证据同时存在，制度到期治理 Agent V1 才能称为完成：配置与目标来自数据库；真实定时或事件入口创建运行；模型根据证据动态选择工具；运行链路持久化；低风险任务可自动创建；高风险动作会暂停等待人工；确认后能够恢复；权限和失败负向场景没有副作用；本地真实 UI、API 与 D1 验收通过。生产完成还需要单独授权后的部署和线上验收。
