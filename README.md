# 知域 · 企业知识中台

> **一个产品经理的 AI 转型实战作品。**
>
> 我从零搭建了这个企业级 AI 知识管理系统——不是写 PRD 交给研发，而是直接用 AI Coding 工具把产品落地为可运行的系统。
>
> 这个项目的真正产出不是代码，是**一套从产品经理视角对 LLM-Agent 的认知框架**：Agent 的行为边界怎么定义、上下文窗口溢出怎么从产品层面治理、什么时候该用规则什么时候该信任模型。

[🔗 在线演示](https://zhiyu-kb.xyz)　　[📦 GitHub](https://github.com/seaninggit/zhiyu-enterprise-kb)

---

## 为什么做这个项目

市场上 AI PM 的岗位描述越来越具体：要懂 RAG、要设计过 Agent、要理解 Token 成本模型。但大多数 B 端产品经理的实际经验停留在"调用 ChatGPT API 做个问答机器人"。

**我决定自己做一个完整的企业级 AI 系统，因为：**

1. 只有亲自处理过上下文窗口溢出，才会在设计多轮对话时主动考虑压缩策略
2. 只有亲眼看到 Agent 连续调用 9 次工具还不停止，才会在设计 tool-use 时加硬截断
3. 只有算过每次 AI 问答的 Token 账单，才会在意图识别环节做前置过滤省掉 30% 无效调用

**这个项目的每一个功能背后，都有一个我先踩进去、再想办法爬出来的坑。**

---

## 我的角色

```
┌─────────────────────────────────────────────────┐
│                  我的工作                        │
│                                                 │
│  需求拆解  ───  将企业知识管理场景翻译为          │
│                Agent 可执行的能力边界             │
│                                                 │
│  架构设计  ───  浏览器本地 AI 还是云 API？        │
│                正则还是语义分类？同步还是 Cron？   │
│                                                 │
│  Agent 编排 ───  7 个工具的职责划分、             │
│                 tool-use loop 终止条件、          │
│                 system prompt 行为约束            │
│                                                 │
│  质量把控  ───  每次变更后验证功能闭环、           │
│                 边界 case 覆盖、成本是否可控       │
│                                                 │
│  迭代决策  ───  每一次提交都在权衡：                │
│                 修 bug vs 加功能 vs 还技术债      │
└─────────────────────────────────────────────────┘
```

技术实现由 **VS Code + Codex Computer Use + DeepSeek-v4-pro** 驱动（Vibe Coding 模式）。

---

## 系统全貌

### 一张图看懂产品架构

```
  用户浏览器（React 19）
  ├─ 本地 OCR (Tesseract.js WASM)       ← 不上传图片到云端
  ├─ 本地向量化 (MiniLM-L12, 384d)      ← 零 API 成本
  └─ 知识管理 / AI 对话 / 治理仪表盘
         │
         ▼
  Cloudflare Workers（边缘计算）
  ├─ 知识生产引擎 ── 解析 → OCR → 切片 → 向量索引
  ├─ 审核发布引擎 ── 草稿 → 部门审核 → 快照发布
  ├─ 权限安全引擎 ── RBAC → 部门隔离 → 行级 ACL
  ├─ AI 问答引擎  ── 意图识别 → 权限检索 → LLM 生成 → 引用溯源
  ├─ Agent 巡检   ── tool-use loop → 7 工具自主决策
  └─ 定时治理     ── Cron 自动作废 / 去重 / 纠错 / 提醒
         │
         ▼
  数据 & 外部服务
  ├─ D1 (SQLite)   ── 18 张业务表，19 个迁移
  ├─ R2 (文件存储) ── 原始附件，权限校验下载
  ├─ DeepSeek API  ── flash 日常问答 / pro 复杂推理
  ├─ DashScope      ── 1024d 中文语义向量
  └─ Resend         ── 事务邮件通知
```

### 产品需求拆解

<details>
<summary><b>点击展开：完整功能模块矩阵</b></summary>

| 模块 | 核心问题 | 怎么做 |
|------|---------|---------|
| **知识生产** | 业务部门如何低门槛贡献知识？ | 多格式拖拽上传 → 自动解析 → 浏览器 OCR → 切片索引 |
| **审核发布** | 如何保证知识质量可追溯？ | 草稿→部门审核→快照发布，驳回记录原因，版本独立管理 |
| **权限隔离** | 敏感知识如何防止越权访问？ | 三级 RBAC + 部门隔离 + 行级安全，前后端双重校验 |
| **AI 问答** | 如何让 LLM 只基于企业知识回答？ | RAG 检索 → 权限过滤 → 引用约束 prompt → 来源核验 |
| **意图识别** | 用户说"你好"时要不要调 LLM？ | 语义分类前置，7 类非知识意图直接返回，省 30% Token |
| **知识治理** | 过期/重复/低质内容怎么持续清理？ | 定时巡检 + 一键操作 + Agent 深度分析 + 反馈闭环 |
| **Agent 巡检** | 能否让 AI 帮管理员治理知识库？ | 7 工具 tool-use loop，自主发现→分析→建议→执行 |
| **决策树** | 员工"报销怎么报"不想等人工回复 | 交互式流程指引，一步步选到答案，引用原文依据 |

</details>

### 技术选型：为什么选它

| 层级 | 选型 | 为什么选它 |
|------|------|-------------|
| 推理 LLM | DeepSeek-v4-flash / pro | 中文企业知识理解优于 GPT-4o-mini，成本为其 1/4，上下文 128K 够用 |
| 向量模型 | DashScope qwen3.7 (1024d) | 中文语义匹配精度高于 OpenAI text-embedding-3，按量付费无月费 |
| 本地向量 | Xenova MiniLM-L12 (384d) | 浏览器端运行，零成本、低延迟，384 维对企业知识够用 |
| 本地 OCR | Tesseract.js WASM | 不上传扫描件到云端，合规友好，中文识别率 90%+ |
| 边缘计算 | Cloudflare Workers | 全球低延迟，免费额度覆盖 MVP 全部流量，零运维 |
| 数据库 | D1 (SQLite) | 边缘 SQL，与 Workers 部署在一起，延迟 < 5ms |
| 邮件 | Resend | 轻量事务邮件，免费 100 封/天，够 MVP 用 |

> **成本策略**：能用浏览器能力的不用云 API（OCR、本地向量化），能用边缘免费的不用服务器（Workers、D1、R2），必须云化的挑性价比最优（DeepSeek 而非 GPT-4）。

---

## LLM-Agent 认知深度

### 一、Agent 不是"把工具列表丢给 LLM"

Agent 看起来很简单：定义几个 function，LLM 自动决定调用哪个。实际上线后我发现，**真正的挑战全在边界控制上**。

**我遇到的问题：**

> 给 Agent 配了 7 个知识管理工具后，用户输入"你好"，它仍然调用 `search_knowledge` 检索一遍，然后再回复"你好"。一次简单问候消耗了 2000+ tokens。

**三层护栏：**

```
第一层 · 意图前置分类
  ├─ 7 类非知识意图（身份/能力/寒暄/告别/感谢/确认/账号）
  ├─ 9 类模板 64 条样例 → DashScope 语义向量匹配
  └─ 命中后直接返回模板答案，不进入 Agent/检索流程
        ↓ 未命中
第二层 · 输入特征门控
  ├─ 长度 > 15 字 + 含业务关键词 → 跳过分类，直接检索
  ├─ 长度 ≤ 1 字且无问号 → 引导用户说出需求
  └─ 含"报销/差旅/合同/入职"等词 → 直接检索
        ↓ 进入 Agent
第三层 · 运行约束
  ├─ MAX_ITERATIONS = 5（硬截断，防止循环失控）
  ├─ batch_archive 单次最多 20 篇（爆炸半径控制）
  ├─ 工具执行结果截断至 500 字符（避免上下文溢出）
  ├─ 写操作（作废、建任务）走 confirm-then-execute 模式
  ├─ 每次工具调用写入审计日志
  └─ 截断时输出已有分析结论，不说"处理超时"
```

**system prompt 是软约束，代码层的护栏才是硬约束。**Agent 行为边界不应该只靠 prompt engineering，需要多层防御架构。**具体来说：工具执行结果必须截断（否则一次 `list_documents` 就能撑爆上下文），写操作必须有人确认（AI 可以建议但不能替人决策），批量操作必须有上限（爆炸半径控制）。

---

### 二、上下文窗口治理：从"超出限制"到"永不超限"

这是我在这个项目中投入最多思考的产品问题，因为 **LLM 的上下文窗口是所有 AI 产品的隐性天花板**。

**实际遇到过的情况：**

5 轮对话后，每轮检索 5 篇文档（每篇 ~800 字摘要），加上 system prompt 和历史消息，第 6 轮 DeepSeek 直接返回 `context_length_exceeded`。

**这不是靠扩大上下文窗口能解决的问题——模型窗口再大，不控制输入量迟早会爆。我做了四层处理：**

```
Layer 1 · 追问检测
  "那需要什么材料？"  → 检测指代词 "那"，判定为追问
  "报销标准是多少？"   → 检测具体关键词，判定为新问题
  追问时缩小检索范围到上一轮的引用文档，新问题全量检索

Layer 2 · 引用溯源过滤
  LLM 生成答案后正则提取所有 [N] 引用编号
  从 5 篇 source 中只保留实际引用的 2-3 篇
  未引用的检索结果在下一轮中不参与上下文拼接
  效果：后续轮次 context 减少 40-60%

Layer 3 · 滑动窗口裁剪
  检索阶段：全量返回 top-5（保证召回）
  生成阶段：只发送最近 15 条消息（保证不超限）
  system prompt 从 1200 tokens 压缩到 600 tokens

Layer 4 · 会话边界识别
  "好的"、"不需要" → 识别为对 AI 追问的回应，不重新检索
  "再见"、"先这样"  → 识别为会话结束，不发起新检索
```

**上下文管理不是工程优化项，**它决定了你的产品能做几轮对话**。从第 1 天就要设计，否则用户在第 6 轮撞墙时已经失去了信任。

---

### 三、Token 成本：每个产品决策都有价格

设计过程中逐步意识到：多带一轮历史消息、多传一篇检索结果、system prompt 多写一段话——每个动作都在消耗 Token。成本控制不是研发的优化项，本质上是产品在决定哪些信息值得占用上下文。

**优化前后对比：**

```
优化前（V1.0 初版）：
  每次问答平均消耗 4,500 tokens
  ├─ system prompt:         1,200 tokens（固定开销）
  ├─ 检索结果 context:      2,000 tokens（5篇 × 400字）
  ├─ 历史消息:              800 tokens（最近 4 轮）
  └─ 生成输出:              500 tokens
  日均 200 次问答 × 4,500 tokens = 90 万 tokens/天

优化后（V1.1，即当前版本）：
  每次问答平均消耗 2,200 tokens（↓ 51%）
  ├─ 意图前置识别省掉 30% 请求              ← 短输入直接返回模板
  ├─ system prompt 压缩至 600 tokens        ← 去掉冗余说明
  ├─ 引用后过滤省掉 40% 无关 context        ← 只保留实际引用的文档
  ├─ 上下文追问时检索范围缩小 60%           ← 沿用上一轮文档范围
  └─ 双模型策略：flash 日常 / pro 复杂推理  ← 成本再降 40%
```

| 优化手段 | 类型 | Token 节省 | 需要判断什么 |
|---------|------|-----------|----------|
| 意图前置分类 | 产品策略 | ~30% 请求绕过 LLM | 哪些意图可以不用模型？阈值设多高？ |
| 引用溯源过滤 | 产品策略 | ~20%/轮 | 怎么判断"引用过"？用正则提取 [N] |
| 追问范围收敛 | 交互设计 | ~40% context | 什么是追问？指代词列表怎么维护？|
| 双模型路由 | 成本策略 | ~40% 复杂场景 | flash 和 pro 的切换条件是什么？|
| system prompt 压缩 | 工程优化 | ~10% | 哪些 prompt 内容是模型已知的？|

**如果你在 PRD 里写了"支持多轮对话"，但你不知道第 6 轮会不会因为上下文溢出直接报错——那你写的需求是不完整的。 **AI PM 的需求文档里应该包含 Token 预算**。

---

### 四、幻觉治理：当 LLM 收到不相关的检索结果

**一个实际例子：**

搜"员工福利标准"，语义向量把"员工手册（含考勤、着装、行为规范）"排到了 top-1。LLM 拿到这份考勤制度后，编了一段"福利包含全勤奖"——这句话在任何企业文档里都不存在。

**针对这个问题，我做了三层处理：**

```
第一道防线 · 检索端
  ├─ 关键词 + 语义双路混合排序（权重 0.28 : 0.42）
  ├─ 标题命中加权（每个关键词命中 +0.06）
  └─ 短语完整命中加分（+0.12）

第二道防线 · 检索后校验
  ├─ 来源相关性检查（areSourcesRelevant）
  ├─ 中文二分词 + 去停用词
  ├─ 标题匹配任一关键词 → 放行
  └─ 否则需要 ≥ 2 个内容关键词命中 → 放行，否则拒答

第三道防线 · 生成后过滤
  ├─ 正则提取答案中的 [N] 引用编号
  ├─ 剔除未被实际引用的 source（避免 UI 误导）
  └─ mode 与 source 矛盾时清空 source 列表（审查一致性）
```

幻觉拒答从"从不拒答"变为该拒就拒，假阳性引用从偶发降为零。

**RAG 不等于"检索到文档→喂给 LLM→输出答案"。**检索到文档只是开始，检索后的过滤和生成后的校验才是质量防线。**

---

### 五、搜索自学习：让系统从用户行为中自我改进

传统搜索系统的问题是：**用户搜了 A 没结果，换成 B 找到了——这个行为数据被丢弃了**。

我设计了一个自动闭环：

```
用户搜索"差旅费保销" → 0 结果
     ↓ 用户换词
用户搜索"差旅费报销" → 找到 5 篇
     ↓ Cron 定时分析
系统发现: 同用户 24h 内, pinyin 相同, 文字不同
     ↓ 自动创建
search_corrections 表新增: "差旅费保销" → "差旅费报销"
     ↓ 下次命中
下一个用户搜"差旅费保销" → 自动纠错 → 直接找到
```

**几个关键取舍**：

| 决策 | 细节 | 原因 |
|------|------|------|
| 显式规则 vs 算法推断 | 管理员配置的纠错规则优先（置信度 0.99），算法推断为辅（置信度 0.93） | 人工规则宁可少，不能错 |
| 纠错阈值 0.92 | 置信度 ≥ 0.92 才应用纠错 | 低于此值宁可不纠，保留原始查询 |
| 频次下限 = 2 | 候选纠错词必须在本库中出现 ≥ 2 次 | 防止纠到生僻术语 |

**最好的 AI 功能不是"用户输入→AI 输出"，而是**用户用着用着，系统自己变好了**。搜索自学习的投入是一次性的（Cron 脚本），收益是永久的（每个用户受益）。

### 六、检索排序的 IDF 设计：为什么"制度"不能参与排序

关键词匹配的常见陷阱是：**高频官僚词汇污染排序**。

"材料"、"制度"、"管理"、"申请"这些词出现在 80% 的企业文档中，如果每个命中都加同样的分，它们会淹没真正的区分性关键词。

**做法——带噪声惩罚的 IDF 加权：**

```
关键词权重 = token IDF × (term 在 chunk 中出现的文档数 / corpus 总数)

对于低信号词（材料、资料、申请、制度、管理…）：
  → 权重 × 0.15（降权 85%）
  → 它们不再主导排序
```

同时设 `hasStrongMatch` 门控：如果用户查询的区分性关键词在 chunk 中一个都没命中，即使语义相似度很高，得分也上限 0.14——防止"跟问题完全无关但向量撞上了"的假阳性。

**搜索排序的权重调优不是研发调参——**什么词算噪声、噪声的惩罚力度多大，都是产品决策**，需要理解用户实际搜索行为和业务语料特征。

---

### 七、Agent 工具调用的协议级别细节

Agent 模式下 LLM 返回 tool_calls，系统执行后以 `role: "tool"` 回传结果。这个看似简单的协议在实际落地时有 3 个致命细节：

| 问题 | 现象 | 怎么做 |
|------|------|---------|
| tool_call_id 错乱 | LLM 忽略 tool result，重复调用同一工具 | 严格按 OpenAPI 协议回传原始 id，不乱序 |
| 温度参数不当 | 低温度 tool 调用过于保守，高温度总结胡说 | 分阶段温度：tool-use 阶段 0.2，总结阶段 0.5 |
| 无终止条件 | Agent 连续调用 9 次工具仍未给出终答 | MAX_ITERATIONS = 5，截断时将已有分析作为终答输出 |

**Tool-use 不是"给个 JSON Schema 就完事了"。你需要理解 **LLM 在什么条件下会重复调用、什么时候需要推它一把让它 stop**。这是 PM 的设计职责，不是研发的实现细节。

---

### 八、从硬编码到语义分类：意图识别的产品演进

**V1.0**：15 条正则表达式硬匹配
- "你是.*" → 身份询问
- "你好.*" → 寒暄
- 覆盖率约 60%，"中午好"、"在吗"、"你叫啥"都匹配不上

**V1.1**：语义向量分类（当前方案）
- 64 条意图模板预计算 1024d DashScope 向量，缓存到 D1
- 用户输入实时向量化（~150ms），余弦相似度匹配最近邻
- 覆盖率 92%，误判率 < 5%

**这个演进路径恰好说明了 AI PM 的核心判断力——**知道什么时候该从规则切换到模型**。规则是精确脆弱的，模型是模糊鲁棒的。选哪个，取决于你的容错率和成本约束。

---

## 环境配置

### 快速启动

```bash
git clone https://github.com/seaninggit/zhiyu-enterprise-kb.git
cd zhiyu-enterprise-kb
npm install
cp .env.example .env.local
```

### 最小可运行配置

```dotenv
# 必填：AI 问答
AI_PROVIDER=deepseek
AI_BASE_URL=https://api.deepseek.com
AI_CHAT_MODEL=deepseek-v4-flash
DEEPSEEK_API_KEY=sk-your-key

# 可选：语义意图分类（不填则降级为正则）
DASHSCOPE_API_KEY=sk-your-key

# 可选：邮件通知（不填则仅站内通知）
RESEND_API_KEY=re_your-key
```

```bash
npm run dev     # 本地开发
npm run build   # 生产构建
npm test        # 集成测试
```

> 不填任何 Key 时，权限过滤、关键词检索、本地向量检索、全文搜索仍完全可用。

---

## 产品功能全景

### 知识全生命周期

```
创建 ──→ 解析 ──→ 审核 ──→ 发布 ──→ 检索 ──→ AI 问答 ──→ 治理 ──→ 审计
  │        │        │        │        │          │          │        │
  │     OCR/切片   驳回     快照    语义+关键词  引用溯源   过期检测   操作记录
  │     PDF/DOCX   退回     版本     权限过滤   多轮对话   重复清理   请求追踪
  │     XLSX/图片  草稿     隔离     纠错建议   办理清单   反馈闭环   敏感操作
  └──────────────────────────────────────────────────────────────────────┘
```

### 核心功能矩阵

| 能力 | 普通员工 | 部门管理员 | 超级管理员 |
|------|---------|-----------|-----------|
| 知识检索 | 本部门已发布 + 共享 | 本部门全部 | 全平台 |
| AI 问答 | ✅ | ✅ | ✅ |
| 上传/编辑 | 本人创建 | 本部门 | 全平台 |
| 审核发布 | — | 本部门 | 全平台 |
| Agent 巡检 | — | 本部门 | 全平台 |
| 定时治理 | — | — | ✅ |
| 系统配置 | — | — | ✅ |

### 7 个 Agent 工具

| 工具 | 用途 | 权限控制 |
|------|------|---------|
| `search_knowledge` | 按关键词检索已发布知识 | 部门可见范围 |
| `inspect_document` | 深度检查单篇文档（版本、审批记录、切片摘要） | 部门可见范围 |
| `list_documents` | 按状态批量列出文档（过期/草稿/待审核） | 部门可见范围 |
| `find_similar` | 标题相似度检测，发现疑似重复 | 部门可见范围 |
| `batch_archive` | 批量作废过期文档 | 本部门管理权限 |
| `create_governance_task` | 创建治理待办 | 本部门管理权限 |
| `send_email` | 发送邮件通知（管理员/文档负责人） | 管理员专用 |

---

## 迭代路线

### V1.0 — 企业知识中台 MVP ✅

从零到可演示。打通知识全生命周期（生产→审核→发布→检索→AI问答→治理），保证闭环完整。知识库只做一件事：**让制度、规范、指引被找到、被信任、被持续维护。**

---

### V1.1 — Agent 主动运营（进行中）

Agent 从被动工具升级为主动运营角色——不用人等，自己跑、自己分析、自己汇报。

| 功能 | 解决的问题 |
|------|-----------|
| **定时主动巡检** | Agent 按 Cron 自动执行巡检，完成后推一条总结到通知中心，管理员不用每次手动点 |
| **异常主动预警** | 不只是"发现 3 份过期"，而是"其中 2 份过去 30 天被 AI 高频引用，建议优先处理"——有优先级判断 |
| **运营周报自动生成** | 每周一汇总：新增文档数、搜索趋势、Agent 自动处理量、待人工处理项 |

> 条件触发（按指标阈值自动执行）放后续迭代。

---

### V1.2 — 知识消费分析

**现状**：`search_logs`、`ai_query_logs`、`ai_answer_feedback` 三张行为表有数据，但只在巡检页面展示几个计数，没有分析价值。

| 功能 | 解决的问题 |
|------|-----------|
| **文档引用热度榜** | 哪些文档被 AI 高频引用 → 识别不能出错的核心制度 |
| **搜索命中率趋势** | 零结果比例在上升还是下降 → 搜索自学习纠错是否真的有效 |
| **知识缺口热力图** | 按部门看哪些主题搜了没结果 → 谁该优先补充资料 |
| **AI 答案采纳率** | 用户看了答案后点"已解决"比例 → 哪些部门的资料 AI 解释不清楚 |

---

### V1.3 — 知识质量信号

**现状**：靠人工判断文档质量，没有量化指标支撑审核决策。

| 信号 | 解决的问题 |
|------|-----------|
| **引用频次 × 采纳率** | "这份文档被引用了 47 次但用户满意度只有 60%"→ 审核人知道该重点修订 |
| **时效性风险** | 离复核日期不到 30 天 → 自动标记，避免过期后 AI 还在引用 |
| **版本活跃度** | 一年没更新的文档 → 提醒复核，区分"稳定有效的制度"和"被遗忘的旧版" |

---

### V1.4 — Prompt A/B 对比评测

**现状**：能跑评测看检索质量，但改 Prompt 后无法对比新版本效果。改 Prompt 影响的是 LLM 怎么组织答案，当前评测只测检索不测生成。

| 功能 | 解决的问题 |
|------|-----------|
| **完整 RAG 评测** | 同一组题，分别跑检索+LLM生成，对比答案质量和引用准确性 |
| **版本对比报告** | V3 vs V2 效果对比一目了然，数据驱动 Prompt 迭代 |
| **回归测试** | 加新文档后自动跑评测，确认没破坏原有检索质量 |

---

### V1.5 — 知识关联图谱

**现状**：文档之间是孤岛。"差旅报销标准"引用了"费用报销制度"，系统不知道。

| 功能 | 解决的问题 |
|------|-----------|
| **引用关系自动发现** | 正文"详见《XX 制度》"→ 自动建立有向引用 |
| **变更影响分析** | 改了 B 制度 → 自动列出所有引用 B 的文档，防止遗漏同步 |
| **多跳检索** | "海外差旅住宿标准"→ 差旅制度→关联住宿标准→定位条款 |

---

### V1.6 — 数据连接器

**现状**：保留入口，标注"规划中"。企业知识散落在飞书文档、钉钉知识库、企业微盘等外部系统中，全靠人手传。

| 功能 | 解决的问题 |
|------|-----------|
| **飞书/钉钉/企微对接** | 外部系统的文档变更自动同步进知识库，不再依赖人手上传 |
| **定期增量同步** | 只拉更新的文档，不重复全量导入 |
| **来源追溯** | 保留原始来源链接和同步时间戳，可核验 |

> 此功能需企业在对应平台注册开发者应用并授权，属于交付实施环节。一期保留架构入口。

---

### V2.0 — 组织知识健康度（远期）

把 V1.2 的消费分析 + V1.3 的质量信号 + V1.5 的关联关系汇总为一个综合视角。

- **知识健康度评分**：时效性 × 引用率 × 采纳率 × 关联完整度 → 单一分数
- **部门知识资产仪表盘**：各部门知识总量、健康度、缺口、趋势
- **Webhook 事件推送**：文档发布/过期/修订时推送到外部系统（企业微信/钉钉通知），一期保留入口

---

### 不做进二期的（产品克制）

| 功能 | 不做原因 |
|------|---------|
| 多源数据连接器（Slack/Confluence/企微） | 需要 OAuth 体系 + 持续维护，1 人团队不可行 |
| IM 机器人 | 企业 IM 审批非技术问题，属商务落地范畴 |
| 替代业务系统的审批/流程引擎 | 知识库边界之外，不应越界 |
| AI 自动生成知识文档 | 幻觉风险不可控，企业合规场景不接受

---

## 产品经理实战复盘

### 为什么一个 PM 要自己动手

AI 产品的需求边界无法提前完全定义。你没法在 PRD 里写：

> "第 5 轮对话如果上下文超限，系统应智能压缩历史消息并重新检索"

写这行需求的前提是，你得先搞清楚为什么 128K 窗口在第 5 轮就超了。不亲手撞一次 `context_length_exceeded`，根本不知道问题出在哪。

### 踩坑 → 产品优化闭环

| 用户/系统问题 | 根因（LLM 层面） | 怎么做 | 设计逻辑 |
|-------------|-----------------|---------|---------|
| 第 5 轮对话报错 | 上下文溢出，历史消息 + 检索结果超限 | 滑动窗口 + 引用过滤 + 追问检测 | 不依赖模型窗口大小，从产品侧做压缩 |
| AI 基于无关文档编造答案 | 检索假阳性，LLM "努力"基于无关 context 回答 | 来源相关性二次校验 + 拒答 | 检索结果不等于可用证据，需要中间校验层 |
| Agent 疯狂调用工具不停 | 没有迭代上限，LLM 倾向于探索而非收敛 | MAX_ITERATIONS = 5 + 截断输出已有结论 | 模型不会主动停止，需要设计强制终止条件 |
| "我是谁"也要调 LLM 生成 | 所有输入统一走 RAG pipeline | 意图前置分类，非知识意图直接返回模板 | 不是所有输入都值得花 Token，分层路由降本 |
| 同一份文档上传两次标题微调 | 没有上传时重复检测 | 标题相似度实时检查 + 警告提示 | 在用户操作时拦截，比事后清理成本低 |
| 用户搜"报销"命中大量文档 | 关键词匹配无排序 | 标题加权 + 噪声词降权 + 混合排序 | 排序权重反映业务理解，不是纯技术调参 |
| 搜索 0 结果→用户换词→找到 | 搜索行为数据浪费 | 自动建立同音纠错映射 | 用户行为是最好的训练数据，闭环利用 |
| "好的谢谢"触发全量检索 | 短确认被当新问题 | 会话状态感知 + AI 追问识别 | 对话不是独立问答的集合，需要状态机管理 |

### 我的核心收获

**1. AI PM 的核心能力不是会用大模型，是知道它在什么条件下会失败。**

每个功能上线前我问自己三个问题：这个输入最长会多长？如果 LLM 返回了错误格式怎么办？如果用户连续这么用 20 次会不会出问题？

**2. Agent 产品设计，工具定义只占 20%，行为边界占 80%。**

工具定义是 20% 的工作量，剩下 80% 是：什么情况下不调用工具（意图前置）？调用多少次必须停止（硬截断）？工具执行失败后给 LLM 什么信号（错误格式）？

**3. AI 产品的边际成本不在服务器，在 Token。**

AI 产品的边际成本不在服务器，在 Token。每个产品决策——是否多带一轮历史消息、是否加一段 system prompt、是否用更大的模型——都有对应的 Token 代价。

**4. MVP 不是做最少的功能，是找最短的验证闭环。**

我第一版只做了三件事：上传→审核→AI问答。验证这条链路跑通后，再依次加上权限、治理、Agent、决策树。每个迭代都在一个完整闭环上叠加，而不是分散做孤立功能。

---

## License

本项目为个人实战作品，当前未声明开源许可证。未经授权，不得用于商业分发。

---

*Built with VS Code + Codex Computer Use + DeepSeek-v4-pro*
